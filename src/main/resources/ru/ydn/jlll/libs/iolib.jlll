(define (println . rest)
  :doc "Prints arguments followed by a newline. Lazy sequences are streamed element by element."
  (for-each print rest)
  (print "\n"))

(define (newline)
  :doc "Outputs a newline character to stdout."
  (print "\n"))
