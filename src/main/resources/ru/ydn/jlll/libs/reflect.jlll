(define (get-classname object) (invoke (invoke object 'getClass) 'getName))

(define (to-string obj) (invoke obj 'toString))

(define (to-symbol str) (invoke-static 'ru.ydn.jlll.common.Symbol 'intern str))

(define jlll-convertors '())